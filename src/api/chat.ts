// Este arquivo não expõe mais rotas próprias — o chat de IA está em /api/ai/chat (ai.ts)
// e o chat público de visitantes está em /api/public/chat/:liveId (public.ts).
// Mantido vazio para não gerar erros de import no server.ts.
